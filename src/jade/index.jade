doctype html
html
  head
    title Leap Music
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge,chrome=1')
    meta(name='description', content='Leap Music')
    meta(name="viewport", content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no")
    if develop
      link(href="css/app.css", type="text/css", rel="stylesheet")
    else
      link(href="css/app-#{version}.min.css", type="text/css", rel="stylesheet")
  body
    .wrapper
      .container
        h3 Leap Music

        p Hello! This is a web application showcasing data handling from a Leap Motion device with the LeapJS framework. Plug in your device and make sure the software is running! <br> Move your hands around over the Leap.
        .outputCon
          #output
    if develop
      each jsFile in jsLibFiles
        script(src="js/#{jsFile}")

    each jsFile in jsFiles
      script(src="js/#{jsFile}")

    if develop
      script(src="//localhost:9001/livereload.js")
